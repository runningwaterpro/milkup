# AGENTS.md

本文件只适用于 `runningwaterpro/milkup`（工作仓库 / fork）。**不要把它推送到上游 `Auto-Plugin/milkup`。**

## 仓库对应关系

| 简称 | 地址 | 角色 |
| --- | --- | --- |
| 工作仓库 / fork | `runningwaterpro/milkup` | 贡献在这里进行，remote 名为 `fork` |
| 主仓库 / 上游 | `Auto-Plugin/milkup` | remote 名为 `origin`，**只能通过 PR 影响它** |

注意 remote 命名容易误读：`origin` 指向的是**上游**。推代码用 `git push fork`，不是 `git push origin`。

## 贡献流程

1. 在 fork 上开发，验证通过
2. 向上游提 PR，等维护者合并

**不要直接推送到上游。** 该仓库没有写权限，也不应该绕过 PR 流程。

## 撰写 PR 描述

提 PR 前必须写描述，提交信息（commit message）同样适用。规则如下。

### 1. 站在用户角度写

读者是准备 review 的人，也是这个功能未来的使用者。先说他能做什么、解决什么，再讲怎么做的。

不要以「我改了哪些文件」「新增了哪些函数」开头。文件列表 GitHub 会自动生成。

### 2. 必备结构

```text
<一句话说清用户能做什么>

怎么用
<按键、菜单、界面的具体操作，按键写成 Ctrl/Cmd + X 的形式>

解决的实际问题
<改之前用户会遇到什么，改之后是什么样。带具体数字和现象>

行为边界
<哪些东西不受影响、范围到哪里、什么情况下会失效>

验证
<跑了什么测试、实测了什么、覆盖了哪些场景>

范围
<明确不包含什么，避免 reviewer 误判>
```

### 3. 每一节的要求

**怎么用** —— 列出所有入口（快捷键、按钮、菜单、滚轮）。快捷键要写实际按键，不是内部的绑定字符串。

**解决的实际问题** —— 必须有可观察的现象。写「300% 时偏移 7380px（约十二屏），选中的文字往上跑、最终移出视口」，不要写「优化了滚动位置计算」。数字和现象让人能判断这个问题是否真的存在过、是否真的修好了。

**行为边界** —— 覆盖这些方面：不改什么（文档内容、文件路径、撤销历史、剪贴板）、作用范围（只影响哪一层）、生效条件（有焦点即可还是需要先点击）、失效情况（到达边界、只读文档、无活动标签）。

**验证** —— 分三层写：自动化测试数量、构建结果、实测数据。实测数据用具体数值和场景，不要写「已验证功能正常」。

### 4. 需要评审确认的部分单独成节

改动了既有行为、占用了他人的快捷键、删除了原有功能，都要单独一节说明：

- 为什么要改（冲突在哪、为什么必须动）
- 影响谁（默认配置下所有用户？只有自定义过的用户？）
- 是否需要迁移
- 附上核对过的依据（例如「已逐一比对现有 25 条默认快捷键与菜单 accelerator，均未占用」）

不要把这些混在「行为边界」里，会被漏看。

### 5. 拆分原则

一个 PR 只做一件事。混进来的独立 bug 修复要拆出去单独提，并在描述里说明依赖关系。

判断标准：reviewer 能否用一个理由接受这个 PR。如果需要两个理由，就该拆。

### 6. 提交信息（commit message）

- 用中文
- 首行 `type: 简述`，type 取 `feat` / `fix` / `refactor` / `chore` / `docs` / `test`
- 正文按上面同样的结构写：怎么用、解决什么问题、行为边界、验证
- 说明改动的原因和取舍，不只说改了什么

### 7. 语言

- 全文中文
- 句子短，一条说一件事
- 不用「我们认为」「显然」「简单来说」这类填充语
- 数字、按键、现象写具体，不写「较多」「很好」「大幅」

## Issue

Issue 和 spec 记录在 `runningwaterpro/milkup`。用 `gh` CLI 时带 `--repo runningwaterpro/milkup`。

## 已知待办

- 窄窗口下导出的 HTML 被压成竖条（既有 bug，与编辑区缩放无关）：见 issue #17
